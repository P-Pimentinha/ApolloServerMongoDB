type Query {
    allUsers: [User] 
}

type User {
    id: ID
    name: String!
    email: String!  
}

input UserInput {
    name: String
    email: String
}

type Mutation {
  addNewUser(user: UserInput): User
}